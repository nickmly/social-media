<% doesUserLikePost = function(id){ 
    var result = false;
     currentUser.likedPosts.forEach(function(post){ 
         if(post._id.equals(id)) {
            result = true;
            return false;
        } 
    }); 
    return result;
} %>

<% doesUserDislikePost = function(id){ 
    var result = false;
     currentUser.dislikedPosts.forEach(function(post){ 
         if(post._id.equals(id)) {
            result = true;
            return false;
        } 
    }); 
    return result;
} %>

<% sortPosts = function(a,b){ 
    if(a.likes > b.likes)
        return -1;
    else if(a.likes < b.likes)
        return 1;
    return 0;
} %>